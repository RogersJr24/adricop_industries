/**
 * Your Railway public HTTPS URL only (no path, no trailing slash).
 * Example: window.RAILWAY_API_BASE = "https://adricopindustries-production.up.railway.app";
 * You can omit "https://" — it will be added automatically.
 * Leave "" when testing only on Railway (same origin as the API).
 */

window.RAILWAY_API_BASE = "https://adricopindustries-production.up.railway.app";
